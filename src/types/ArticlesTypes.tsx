export type ArticleCardResume = {
  slug: string,
  title: string,
  canonicalUrl: string,
  description: string,
  substances: string[],
  specialites: string[],
  pathologies: number[],
  atc: number[],
}